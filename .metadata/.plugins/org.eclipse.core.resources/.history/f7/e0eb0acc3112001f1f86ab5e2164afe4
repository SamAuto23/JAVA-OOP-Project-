package Shop;

public class Keyboard extends Product {
    private Layout layout;

    public Keyboard(int barcode, String brand, String color, ConnectivityType connectivity,
                    int quantityInStock, double originalCost, double retailPrice, ProductCategory category, Layout layout) {
        super(barcode, brand, color, connectivity, quantityInStock, originalCost, retailPrice, category);
        this.layout = layout;
    }

    @Override
    public String displayWithoutCost() {
        return String.format("%d, %s, %s, %s, %s, %s, %d, %.2f, %.2f, %s",
                getBarcode(), getCategory().toString().toLowerCase(), "flexible", getBrand(), getColor(), getConnectivity().toString().toLowerCase(),
                getQuantityInStock(), getRetailPrice(), getRetailPrice(), layout.toString());
    }

    @Override
    public String toString() {
        return String.format("%d, %s, %s, %s, %s, %s, %d, %.2f, %.2f, %s",
                getBarcode(), getCategory().toString().toLowerCase(), "flexible", getBrand(), getColor(), getConnectivity().toString().toLowerCase(),
                getQuantityInStock(), getOriginalCost(), getRetailPrice(), layout.toString());
    }

    // Getters and setters
    public Layout getLayout() {
        return layout;
    }

    public void setLayout(Layout layout) {
        this.layout = layout;
    }
}
